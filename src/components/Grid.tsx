'use client';

import React from 'react';

const Grid: React.FC = () => {
    return (
        <div className="animated-grid-background">
            <div className="animated-grid" />
        </div>
    );
};

export default Grid;
